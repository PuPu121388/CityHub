# CityHub 环境启动清单

> 目的：把跑 `SeckillConcurrencyTest`（并发压测）所需的三件套拉起来。
> 记录的是一台 Windows 机器上的实际路径与坑，不是通用教程。

---

## 0. 依赖总览

| 依赖 | 端口 | 本机形态 | 备注 |
|---|---|---|---|
| MySQL | **3306** | Docker 容器（独立） | 库名 `dingping`，见 `db/hmdp.sql` |
| Redis | 6379 | Docker 容器（复用 DOVideo） | 密码 `9b09b97b22181283`，与 `application.yaml` 一致 |
| RocketMQ | 9876 / 10911 | 本机 `D:\rocketmq` bat 脚本 | 已适配 JDK21 |

**先决条件**：Docker Desktop 必须处于 Running 状态（本次排查时它没运行，所以 Redis 容器起不来）。

---

## 1. 启动 Docker Desktop

```bash
# 确认 daemon 活了（返回容器列表即正常）
docker ps
```

若报 `failed to connect to the docker API at npipe:...`，说明 Docker Desktop 没启动，去开始菜单手动打开，等托盘图标变绿。

---

## 2. 启动 Redis（复用 DOVideo 的容器）

无需新建。DOVideo 的 compose 里定义了 `redis` 服务，端口正好是 6379、密码也已与 CityHub 对齐：

```bash
cd /d/code/DOVideo-AI-main/DOVideo-AI-main
docker compose up -d redis
```

> ⚠️ **别删这个容器**——它是 DOVideo 项目的依赖。
> CityHub 只是共用这个 Redis 实例，两者的 key 前缀不冲突
> （CityHub 用 `cache:shop:` / `seckill:` / `login:` / `rate:limit:`，DOVideo 用自己的）。
> 也**不要对它执行 `FLUSHALL`**，会误伤另一个项目的数据。

验证：

```bash
docker exec -it dovideo-ai-main-redis-1 redis-cli -a 9b09b97b22181283 ping
# 期望输出：PONG
```

---

## 3. 启动 MySQL（为 CityHub 独立起一个）

**不建议复用 DOVideo 的 MySQL**：它的端口映射是 `3307:3306`，与 CityHub 配置的 3306 对不上；
且它的 volume 属于 DOVideo，把 `hmdp.sql` 的十几张 `tb_*` 表塞进去会污染另一个项目的数据。

独立起一个，端口正好 3306、root 密码与 `application.yaml` 里写死的 `290390` 一致：

```bash
docker run -d --name cityhub-mysql \
  -e MYSQL_ROOT_PASSWORD=290390 \
  -e MYSQL_DATABASE=dingping \
  -p 127.0.0.1:3306:3306 \
  -v cityhub-mysql-data:/var/lib/mysql \
  mysql:8.0 \
  --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
```

等待就绪（首次初始化约 20~30 秒）：

```bash
# 反复执行直到输出 mysqld is alive
docker exec cityhub-mysql mysqladmin ping -uroot -p290390
```

> 容器已存在时（第二次启动）用 `docker start cityhub-mysql` 即可，不要重复 `docker run`。

---

## 4. 导入数据

`hmdp.sql` 里只有建表 + 基础数据，**不含建库语句**，所以靠上面 `MYSQL_DATABASE=dingping` 建库：

```bash
cd /d/code/CityHub-main/CityHub-main
docker exec -i cityhub-mysql mysql -uroot -p290390 dingping \
  < src/main/resources/db/hmdp.sql
```

验证表和数据量：

```bash
docker exec cityhub-mysql mysql -uroot -p290390 dingping \
  -e "SHOW TABLES; SELECT COUNT(*) AS user_count FROM tb_user;"
```

> ⚠️ **压测依赖用户数**：`SeckillConcurrencyTest` 会取 1000 个用户登录取 token，
> 并要求 `用户数 > 库存数(100)`。若 `tb_user` 数据不足，测试会直接断言失败并提示。
> 需要更多用户时自行 `INSERT` 或用压测脚本里的 SQL 批量造。

---

## 5. 启动 RocketMQ

用本机脚本，双击即可（或命令行）：

```bash
# 窗口 1：NameServer
cmd //c "D:\rocketmq\start-namesrv.bat"

# 窗口 2：Broker（等 NameServer 起来后再执行）
cmd //c "D:\rocketmq\start-broker.bat"
```

两个脚本会各开一个 `cmd /k` 窗口，**不要关掉**。

> 本机脚本已做 JDK21 适配（G1 替代 CMS、移除 `-XX:+UseBiasedLocking` / `-Xloggc`、堆内存调小），
> 否则 JDK21 下起不来。
>
> **不用 Docker 版**：DOVideo 的 compose 里有 `rmqnamesrv` / `rmqbroker`（5.3.4），
> 但那是另一套、且与本机 5.1.4 版本不一致，两个都起会抢 9876 端口。选一套即可。

---

## 6. 一键验证三件套

```bash
python -c "
import socket
for name, port in [('MySQL',3306),('Redis',6379),('RocketMQ-NS',9876),('RocketMQ-Broker',10911)]:
    s = socket.socket(); s.settimeout(1.5)
    r = s.connect_ex(('127.0.0.1', port))
    print(f'{name:16} :{port:<6} ' + ('OPEN' if r==0 else 'CLOSED'))
    s.close()
"
```

四个全 `OPEN` 才继续。

---

## 7. 启动应用

IDEA 里跑 `HmDianPingApplication`。

> **改过 Java 配置类后必须 Rebuild Project**，否则跑的是旧 class（表现为改了不生效）。

看到 Tomcat 起在 8081、RocketMQ producer/consumer 注册成功即可。

---

## 8. 跑压测

IDEA 里运行 `src/test/java/com/hmdp/SeckillConcurrencyTest.java`。

预期日志：

```
压测券已就绪，voucherId: xxx，库存: 100
令牌准备完成，竞争并发数: 1000
压测完成：成功 100，失败 900
✅ 压测通过：并发 1000 抢 100 库存，恰好成交 100 笔，无超卖、无重复下单
压测数据已清理：voucherId: xxx，删除订单 100 条
```

测试会自动清理产生的券与订单，**无论成功失败都会清**（`finally` 块）。

---

## 9. 收工

```bash
# 停 RocketMQ：直接关掉那两个 cmd 窗口

# 停 Docker 容器
docker stop cityhub-mysql
docker stop dovideo-ai-main-redis-1   # 视 DOVideo 是否需要而定
```

保留 `cityhub-mysql` 容器和它的 volume，下次 `docker start cityhub-mysql` 即可，数据还在。

---

## 附：排查过的坑

| 现象 | 原因 | 处理 |
|---|---|---|
| `docker ps` 连不上 daemon | Docker Desktop 没启动 | 手动启动并等托盘变绿 |
| MySQL 连 3306 失败 | DOVideo 的 MySQL 映射到 3307 | 用本清单的独立容器（3306） |
| Redis 认证失败 | 容器带 `--requirepass` | 密码 `9b09b97b22181283`，与 `application.yaml` 已对齐 |
| RocketMQ 起不来 | JDK21 不兼容老 GC 参数 | 本机 `runserver.cmd` / `runbroker.cmd` 已改好，用 `D:\rocketmq` 的脚本 |
| 端口 9876 被占 | Docker 版和本机版同时起 | 只选一套（本清单选本机版） |
